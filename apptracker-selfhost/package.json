{
  "name": "apptracker-selfhost",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "pages:dev": "wrangler pages dev public --compatibility-date=2026-07-04 --d1=DB=apptracker_selfhost",
    "pages:deploy": "wrangler pages deploy public --project-name=monet-apptracker",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute apptracker_selfhost --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
